# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Local project snapshots. Never track these: a renamed pattern once
# un-ignored the file it was protecting and swept a 33.8 MB archive
# (node_modules, dist, a nested .git) into a PR.
*.zip

# Editor / agent local config
.claude/

# Local environment (never commit secrets)
.env
.env.local
.env.*.local

# Tool-vendor metadata
.agents/
skills-lock.json

# Pre-optimization screenshot originals (regenerable; ~15MB)
scripts/.originals/
